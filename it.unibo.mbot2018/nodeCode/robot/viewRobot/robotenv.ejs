<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title><%= title %></title>
<link rel="stylesheet" href="/stylesheets/style.css">
<script type="text/javascript" src="/socket.io/socket.io.js"></script>
<script>
       var socket  = io.connect();
       socket.on('connect', function(){ console.log("socket connected");});
       socket.on('message', function(v){ document.getElementById('display').innerHTML=v; });
</script>
 
</head>

<body>
<div class="container">
<h1><%= title %></h1>

<!-- 
<p><%= res %></p> 
<p>Description= <%= model.description %>  <%= Object.keys(model) %>  </p>

<h3>Properties of the environment</h3>
<p>  <%= Object.getOwnPropertyNames(model) %>  </p>

<ul>
<% Object.keys(model).forEach( function(key)  { %>
   <li><%= key  %></li>
    
 	<% Object.keys( key ).forEach( function(item)  { %>
 	<%= item  %>
 	<% } );%>
 	 
<% } );%>
</ul>
-->

<h3>Devices in the environment</h3>
<ul>
<% Object.keys(model.devices.resources).forEach( function( dev )  { %>
   <li><b><%= dev  %></b> <br/> 
       name=<%= model.devices.resources[dev].name %>, 
       descr=<%= model.devices.resources[dev].description %></li>
   
 <% } );%>
</ul>
 
<p>
Back to main page: <a href="http://<%=host%>">main page</a>  
</p>
</div>
</body>